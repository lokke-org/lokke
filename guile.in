#!/usr/bin/env bash

export GUILE_LOAD_PATH="${GUILE_LOAD_PATH:+$GUILE_LOAD_PATH:}$(pwd)/mod"
export LTDL_LIBRARY_PATH="${LTDL_LIBRARY_PATH:+$LTDL_LIBRARY_PATH:}$(pwd)/lib/.libs"

exec guile-@GUILE_EFFECTIVE_VERSION@ "$@"
